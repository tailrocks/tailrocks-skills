interface:
  display_name: "Tailrocks: Swift Project Audit"
  short_description: "Audit a Swift project baseline read-only"
  default_prompt: "Use $tailrocks-swift-project-audit to inspect this native Swift project read-only and emit the fixed baseline gap ledger without installing or editing."
policy:
  allow_implicit_invocation: false
