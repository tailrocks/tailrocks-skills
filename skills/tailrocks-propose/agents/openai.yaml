interface:
  display_name: "Propose"
  short_description: "Enrich a rough idea into an evidence-backed proposal"
  default_prompt: "Use $tailrocks-propose for this substantial ambiguous idea: gather evidence, recommend one default direction, include only genuine alternatives, and stop before implementation planning."
policy:
  allow_implicit_invocation: false
