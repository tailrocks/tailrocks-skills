interface:
  display_name: "Tailrocks: Remediate"
  short_description: "Apply one approved root-cause correction"
  default_prompt: "Use $tailrocks-remediate to apply this exact approved root-cause correction and prove both the instance and bounded defect class are prevented."
policy:
  allow_implicit_invocation: false
