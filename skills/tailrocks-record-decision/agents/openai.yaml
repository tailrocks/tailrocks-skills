interface:
  display_name: "Record Decision"
  short_description: "Record and propagate one decision on a roadmap item"
  default_prompt: "Use $tailrocks-record-decision to record this decision on the named roadmap item and reconcile the item with it."
policy:
  allow_implicit_invocation: false
