interface:
  display_name: "Tailrocks: Agents Instructions"
  short_description: "Place agent rules in the file that owns them; AGENTS.md is the source"
  default_prompt: "Use $tailrocks-agents-md to assess instruction-file topology and rule placement for work already in scope. Stay read-only unless the active task explicitly authorizes add or sync mutation."
policy:
  allow_implicit_invocation: true
