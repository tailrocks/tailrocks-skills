interface:
  display_name: "Research"
  short_description: "Deep-research a direction into a detailed handoff plan"
  default_prompt: "Use $tailrocks-research on a substantial confirmed proposal direction to resolve material uncertainty and write sufficient zero-context handoff plans."
policy:
  allow_implicit_invocation: false
