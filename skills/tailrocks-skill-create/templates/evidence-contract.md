# Skill evidence contract

- Schema: `tailrocks.skill-evidence/v1`
- Skill: `<skill-name>`
- Source SHA: `<40-character commit SHA>`
- Recorded date: `<YYYY-MM-DD>`
- Provenance: `<field record, controlled baseline, contract check, or security obligation>`

## Evidence

- Observed failure: `<observable failure>`
- Baseline/control: `<artifact or command and result>`
- Required behavior: `<single observable outcome>`
- Acceptance checks: `<commands, checkers, or frozen rubric>`

## Operational contract

- Inputs:
- Preconditions:
- Output and destination:
- Postconditions:
- Failure branches:
- Authority:
- Side effects:
- Retry limits:
- Recovery:
- Idempotency:
- Secret handling:

Use `NOT APPLICABLE — <reason>` only when a field cannot apply.
