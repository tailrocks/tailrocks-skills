interface:
  display_name: "Tailrocks: Web Design"
  short_description: "Design TanStack screens as blessed in-app design routes"
  default_prompt: "Use $tailrocks-web-design to apply web visual-design policy to work already in scope. Do not write design routes, bless, freeze baselines, capture, or mutate production without active-task authority and the required user decision."
policy:
  allow_implicit_invocation: true
