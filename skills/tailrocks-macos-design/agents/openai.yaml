interface:
  display_name: "Tailrocks: macOS Design"
  short_description: "Design a macOS feature and bless it as a running Liquid Glass prototype"
  default_prompt: "Use $tailrocks-macos-design to apply macOS visual-design and Liquid Glass policy to work already in scope. Do not write artifacts, bless, freeze, capture, or mutate production without active-task authority and the required user sign-off."
policy:
  allow_implicit_invocation: true
