interface:
  display_name: "Tailrocks: Rust Project Setup"
  short_description: "Scaffold a strict, modern Rust workspace and tooling"
  default_prompt: "Use $tailrocks-rust-project-setup to scaffold this new Rust project's workspace layout, strict lints, rustfmt, toolchain, mise, and supply-chain and test gates."
policy:
  allow_implicit_invocation: false
