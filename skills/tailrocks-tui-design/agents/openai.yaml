interface:
  display_name: "Tailrocks: TUI Design"
  short_description: "Design terminal UI screens as blessed ratatui golden frames"
  default_prompt: "Use $tailrocks-tui-design to apply terminal visual-design policy to work already in scope. Do not write gallery or golden artifacts, bless, freeze, capture, or mutate production without active-task authority and the required user decision."
policy:
  allow_implicit_invocation: true
