$ git log --format='%h %ad %s%n%b' --date=short --grep='Tailrocks-Skill: tailrocks-audit'

4b7d1e0 2026-07-14 docs(roadmap): audit — 1 item seeded, 2 candidates dropped

    Dropped: "switch the ORM to a query-builder" (direction lane).
    Rejected: contradicts docs/adr/0002-data-layer.md, which decided the
    current data layer and its tradeoffs deliberately. Do not resurface
    without new evidence that contradicts that decision.

    Dropped: "add a second HTTP framework for the admin routes"
    (agent-legibility lane). Rejected: the admin routes were folded into
    the existing service in commit 9c1f0ab, so the evidence is stale.

    Tailrocks-Skill: tailrocks-audit
