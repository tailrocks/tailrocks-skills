interface:
  display_name: "Merge PR"
  short_description: "Merge a pull request fail-closed through the repo's gates"
  default_prompt: "Use $tailrocks-merge-pr to run the pre-merge gates and merge this pull request."
policy:
  allow_implicit_invocation: false
