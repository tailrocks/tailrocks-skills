# Specification

### Requirement: Session boundary

The application MUST use the command-line session boundary.

#### Scenario: Read a snapshot

- **WHEN** a session is selected
- **THEN** the boundary provides its snapshot.
