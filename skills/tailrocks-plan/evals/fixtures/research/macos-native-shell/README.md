# Native macOS shell

The companion is a SwiftUI-first native shell over a Rust-owned session runtime.
The shell owns platform mechanisms only; session policy remains in the CLI core.
Keyboard navigation and native accessibility are required acceptance evidence.
