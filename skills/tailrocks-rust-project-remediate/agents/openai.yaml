interface:
  display_name: "Tailrocks: Rust Project Remediate"
  short_description: "Close approved Rust workspace baseline gaps"
  default_prompt: "Use $tailrocks-rust-project-remediate to close these explicitly approved Rust workspace baseline gaps in buildable, verified slices."
policy:
  allow_implicit_invocation: false
