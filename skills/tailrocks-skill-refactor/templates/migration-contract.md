# Skill migration contract

- Schema: `tailrocks.skill-migration/v1`
- Migration: `<slug>`
- Source SHA: `<40-character commit SHA>`
- Recorded date: `<YYYY-MM-DD>`
- Provenance: `<architecture evidence or failing contract/security check>`
- Authority: `<approver and approved contract loss>`

## Contract

- Source contracts:
- Target contracts:
- Old-to-new invocation mappings:
- Responsibility mappings:
- Artifact mappings:
- Permission and side-effect mappings:
- Compatibility window:
- Acceptance checks:
- Rollback commands and conditions:
- Removal conditions:
- Secret handling:

Every old route maps to exactly one target. Missing, duplicate, or orphan mapping
blocks execution. This artifact is a handoff; refactor never executes it.
