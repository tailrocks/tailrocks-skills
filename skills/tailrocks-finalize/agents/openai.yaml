interface:
  display_name: "Finalize"
  short_description: "Closing interview that earns a roadmap item its READY status"
  default_prompt: "Use $tailrocks-finalize on this roadmap item: interview me to closure — screens, flows, every open question — and set it READY only when the checklist passes."
policy:
  allow_implicit_invocation: false
