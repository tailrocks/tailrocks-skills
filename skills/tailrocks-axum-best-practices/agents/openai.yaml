interface:
  display_name: "Tailrocks: Axum Best Practices"
  short_description: "Build production Axum HTTP services"
  default_prompt: "Use $tailrocks-axum-best-practices to review this Axum service for HTTP boundaries, errors, Tower middleware, security, lifecycle, async ownership, and tests."
policy:
  allow_implicit_invocation: true
