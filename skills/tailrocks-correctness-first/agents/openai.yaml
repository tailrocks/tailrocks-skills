interface:
  display_name: "Correctness-First Remediation"
  short_description: "Redesign proven defect classes without cost tradeoffs"
  default_prompt: "Use $tailrocks-correctness-first to derive the greenfield architecture that prevents this proven defect class, then choose the complete structural correction without optimizing for price, time, effort, or implementation size."
policy:
  allow_implicit_invocation: false
