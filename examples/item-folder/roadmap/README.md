# Roadmap

| Slug | Title | Status | Remaining |
|------|-------|--------|-----------|
| [goal-live-status](goal-live-status/README.md) | Goal Live Status | IN EXECUTION | 1 |

`Remaining` is the count of open statements in the item's Remaining section, or
`—` before anything was verified. The index is a board, not a store: one row per
item, all content in the item, and when it changed is `git log`.
