# Advisor Plans — tailrocks-skills

Generated by the improve skill (deep audit) on 2026-07-23 at commit
`f2c4be5`. Written to `advisor-plans/` deliberately: this repo's own
delivery pipeline claims the `plans/` namespace for implementation
packages.

Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Executor-contract fixes (/goal loop finishable) | P1 | M | — | DONE |
| 002 | Completeness-chain fixes (ledger→spec→plans) | P1 | M | 001 (soft: same files) | DONE |
| 003 | Validator hardening + catalog truth | P1 | M | — | DONE |
| 004 | CI workflow running the validator | P1 | S | 003 | DONE |
| 006 | Injection & secrets hardening (14 skills + GOAL surfaces) | P1 | S | — | DONE |
| 005 | Eval fixtures + judgeable expectations | P2 | M | — (003 composes) | DONE |
| 007 | Stack-skill corrections (6 confirmed defects) | P2 | M | — | BLOCKED — registry latest router-devtools is 1.167.0; no required 1.170.x release exists |
| 008 | External-claims verification (bun ci, vite, shadcn, renovate) | P2 | S | — | DONE — TRUE: bun ci, Vite tsconfigPaths, base-nova, both Renovate keys |
| 009 | Canonical shared rules across the delivery family | P2 | M | 001, 002, 006 | DONE |
| 010 | Contributor DX (PR workflow, Bun bootstrap, pinning, release) | P2 | S | — | DONE |
| 011 | Worked example plan package (quality anchor) | P2 | L | 001, 002 | DONE |
| 017 | tailrocks-contribute — OSS-contribution skill | P2 | L | soft: 003, 006, 009 | DONE |
| 016 | PARKED exit + assumption lifecycle | P3 | S | 001, 002 | TODO |
| 012 | Pipeline walkthrough doc | P3 | M | 011 (soft) | TODO |
| 013 | Eval-runner design spike | P3 | M | 005 (soft) | TODO |
| 014 | Effort-tiers design spike | P3 | M | after 001/002/009 | TODO |
| 015 | Brownfield on-ramp design spike | P3 | L | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason)
| REJECTED (with one-line rationale — finding fixed independently or
approach abandoned)

## Dependency notes

- 002 after 001: both edit `skills/tailrocks-plan/SKILL.md` and its
  references — sequencing avoids merge friction; content is independent.
- 004 requires 003: the workflow runs `bun test scripts/`, created by 003.
- 009 requires 001, 002, 006: it normalizes wording those plans first
  correct/insert; running it early would be re-done.
- 011 requires 001, 002: the example must demonstrate the corrected
  contract (completion predicate, protocol writes, `B#` prefix,
  baseline-first slicing).
- 016 requires 001 (shared edits in `tailrocks-reconcile/SKILL.md` and
  `goal-handoff.md`) and 002 (`A#` ledger context).
- 013 soft-requires 005 (fixture/`files` convention); 012 soft-requires
  011 (links its example); 014 diffs against post-009 text.
- 007 and 008 are disjoint by file — either order, parallel safe.
- 017 is a new-skill build: soft on 003 (validator enforces its files),
  006/009 (uses the canonical boundary sentence), 010 (PR workflow).
  Its plan inlines the full research basis (three web-research passes +
  full analysis of the local tesslio/good-oss-citizen clone), so the
  executor needs no external research. 013's runner should pick up its
  8 eval cases.

## Findings considered and rejected

- `curl | bash` for the Antigravity CLI install (INSTALL.md:188):
  vendor-official path with a brew alternative listed alongside — not a
  repo defect. (Plan 010 adds pinning notes around it anyway.)
- `.gitignore` `target/`/`tmp/` entries: plausible guard against
  committing artifacts from locally scaffolded test projects — harmless.
- Resolver-script security: reviewed clean (HTTPS to canonical
  registries, `encodeURIComponent`, no shell, no file writes).
- Template security defaults: positive findings (bunfig
  `minimumReleaseAge`, empty `trustedDependencies`, strict deny.toml,
  no-automerge renovate) — recorded so nobody "fixes" them.
- "Cold reviewer can't verify two quality-bar items" — largely a
  misreading (the quality bar is the orchestrator's acceptance list);
  the residual access ambiguity is fixed by plan 002 step 6.
- Guard-sentence presence across all 14 descriptions: verified complete
  — the compensating control for Amp/OpenCode/Antigravity holds today;
  plan 003 makes the validator enforce it.
- A dedicated `record-assumption` skill: rejected in favor of wiring the
  assumption lifecycle into existing skills (plan 016's scope note).

## Audit scope note

Deep audit at `f2c4be5`, 8 parallel auditors + first-hand vetting of
every finding that made a plan. Not audited: live client install
behavior (Codex/Grok/Kimi/agy against current binaries) and the truth of
four external-tool claims — plan 008 exists to verify those before
fixing.
